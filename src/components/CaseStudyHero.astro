---
import LinkButton from './LinkButton.astro'

interface Cta {
  href: string
  label: string
}

interface Props {
  tags: string[]
  title: string
  subtitle: string
  primaryCta: Cta
  secondaryCta: Cta
}

const { tags, title, subtitle, primaryCta, secondaryCta } = Astro.props
---

<div class="bg-surface-warm py-14 md:py-30">
  <div class="container md:px-16">
    <div class="grid grid-cols-1 items-center gap-10 lg:grid-cols-2">
      <div>
        <ul role="list" aria-label="Project categories" class="tag-list mb-3">
          {tags.map((item) => <li class="eyebrow">{item}</li>)}
        </ul>

        <h1 id="cs-title" class="hero-title mb-4">
          {title}
        </h1>

        <p class="hero-subtitle mb-7 max-w-xl">{subtitle}</p>

        <div class="flex flex-wrap gap-4">
          <LinkButton href={primaryCta.href} label={primaryCta.label} external />
          <LinkButton
            href={secondaryCta.href}
            label={secondaryCta.label}
            variant="secondary"
            external
          />
        </div>
      </div>

      <slot />
    </div>
  </div>
</div>
