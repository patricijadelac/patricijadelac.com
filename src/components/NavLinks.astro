---
import { Icon } from 'astro-icon/components'

const currentPath = Astro.url.pathname
const navLinks = [
  { href: '/work', label: 'Work' },
  { href: '/methodology', label: 'Methodology' },
  { href: '/writing', label: 'Writing' },
  { href: '/about', label: 'About' },
]
---

{
  navLinks.map((link) => (
    <li>
      <a
        href={link.href}
        aria-current={currentPath === link.href ? 'page' : undefined}
        class="text-text hover:text-text-muted text-sm no-underline transition-colors"
      >
        {link.label}
      </a>
    </li>
  ))
}

<li>
  <a
    href="https://www.linkedin.com/in/patricijadelac/"
    class="bg-text inline-flex items-center gap-1 rounded-full px-5 py-2 text-sm font-semibold text-white no-underline hover:opacity-85"
    target="_blank"
    aria-label="LinkedIn (opens in new tab)"
    rel="noopener noreferrer"
  >
    LinkedIn
    <Icon name="lucide:arrow-up-right" class="h-4 w-4" aria-hidden="true" />
  </a>
</li>
